<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_milestone_test_wizard_form" model="ir.ui.view">
            <field name="name">milestone.test.wizard.form</field>
            <field name="model">milestone.test.wizard</field>
            <field name="arch" type="xml">
                <form string="Milestone Test Wizard">
                    <sheet>
                        <group>
                            <field name="project_id"/>
                            <field name="action_type"/>
                            <field name="milestone_id" invisible="action_type != 'notification'"/>
                            <field name="checkpoint_name" invisible="action_type != 'checkpoint'"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_execute" string="Execute" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_milestone_test_wizard" model="ir.actions.act_window">
            <field name="name">Milestone Test Wizard</field>
            <field name="res_model">milestone.test.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo> 