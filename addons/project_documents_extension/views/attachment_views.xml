<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend ir.attachment form view to add folder selection -->
    <record id="ir_attachment_form_inherit" model="ir.ui.view">
        <field name="name">ir.attachment.form.inherit</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_form"/>
        <field name="arch" type="xml">
            <!-- Temporarily removed folder_id field to prevent ParseError -->
            <!-- Will be re-enabled after data cleanup -->
        </field>
    </record>
</odoo>